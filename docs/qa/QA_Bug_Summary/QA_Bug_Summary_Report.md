# QA Bug Summary Report: Test Case Documentation

**Phạm vi:** Các lỗi phát hiện và đã sửa trong quá trình bổ sung/rà soát tài liệu test case, gồm 2 nhóm: (1) lỗi trong chính 3 file tài liệu (`SU26SE102-GSU26SE52_QA_TestCases.xlsx`, `Report5_Test_Report.xlsx`, `Report5_TestDocumentation.docx`) và script sinh tài liệu đi kèm (`gen-testcases.js`, `merge-into-live.js`, `fill-functions-statistics.js`); (2) lỗi thật trong sản phẩm (dead button, race condition, thiếu xử lý lỗi) được phát hiện qua việc đọc/đối chiếu từng file `tests/unit/*.test.ts(x)` để viết tài liệu, và đã được sửa trong code thật (xác minh qua test tự động đang pass). Không bao gồm sequence diagram.

**Phương pháp:** Đối chiếu số liệu/nội dung trong 3 file tài liệu với trạng thái thật của test suite (`npx vitest list`, `pytest --collect-only`) và với chính các file `tests/unit/*.test.ts(x)` / `RAG_IQGS/tests/*.py` trên đĩa.

---

## Bug đã phát hiện và sửa

| # | Bug | Vị trí | Nguyên nhân | Cách sửa | Trạng thái |
|---|---|---|---|---|---|
| 1 | **210 sheet trùng lặp trong `SU26SE102-GSU26SE52_QA_TestCases.xlsx`**: thay vì ~113 sheet đúng, file có 210 sheet (gần gấp đôi) sau một lần chạy pipeline sinh test case. | `docs/qa/gen-testcases.js` (`sheetNameFor()`), phát hiện qua `merge-into-live.js` | `sheetNameFor()` dùng một `ID_PREFIX_MAP` để đặt tên sheet theo ID, khác với quy ước đặt tên bằng bare method-name mà ~97 sheet cũ trong file thật đang dùng. `merge-into-live.js` so khớp tên sheet chính xác (exact match) để xoá sheet cũ trước khi thêm sheet mới. Vì tên không khớp, nó không nhận diện được sheet cũ nên chỉ **cộng dồn** sheet mới lên trên, không thay thế. | Khôi phục từ backup, bỏ hẳn `ID_PREFIX_MAP`, dùng lại `tc.method` trần (cắt 31 ký tự, sanitize) đúng quy ước cũ. Sinh lại toàn bộ, xác minh còn đúng 113 sheet, 0 trùng tên. | Đã sửa |
| 2 | **17 dòng trùng lặp (BE_API_002–018) trong sheet Functions/Statistics**: dữ liệu cũ từ một lần chạy trước (với `testCases.length` khác) không được xoá khi mảng test case thay đổi kích thước ở lần chạy sau. | `docs/qa/fill-functions-statistics.js` | Script chỉ ghi đè các dòng nằm **trong** phạm vi dữ liệu hiện tại (`dataStart..dataEnd`), không xoá các dòng còn sót lại **phía sau** phạm vi đó nếu lần chạy trước có nhiều dòng hơn. | Thêm logic dọn dẹp phòng thủ: sau khi ghi xong cả 2 sheet Functions và Statistics, quét tiếp các dòng phía sau `dataEnd`, xoá giá trị + border cho tới khi gặp dòng trống. Xác minh lại 0 ID trùng trong Functions sau mỗi lần chạy pipeline. | Đã sửa |
| 3 | **Số liệu pytest bị lỗi thời trong tài liệu test case**: một file ghi "20 pytest, chỉ 1 file" trong khi số thật là **39 pytest case** trải trên **2 file** (`test_e2e_api.py`: 24 case, `test_ollama_integration.py`: 9 hàm, một số có parametrize). | `SU26SE102-GSU26SE52_QA_TestCases.xlsx` (notes/summary row) | Số liệu được ghi tại thời điểm RAG_IQGS chỉ có 1 file test, không được cập nhật sau khi `test_ollama_integration.py` được thêm vào. | Chạy `pytest --collect-only -q tests/` để lấy số thật (39 case / 2 file), cập nhật lại dòng ghi chú trong `gen-testcases.js` để khớp với `Report5_TestDocumentation.docx` (vốn đã đúng). | Đã sửa |
| 4 | **3 mục `AUTOMATION_STATUS` bị đánh dấu "SKIPPED" trong khi thực tế đã có test tự động thật**: `FE_UI_014`, `FE_UI_015`, `FE_RAGAUTH_016` đều đã được cover bởi test case thật (`admin-manage-users.test.tsx`, `info-tooltip.test.tsx`, `candidate-forbidden.test.tsx`) nhưng bảng trạng thái tự động hoá vẫn ghi "chưa làm". | `docs/qa/gen-testcases.js` (`AUTOMATION_STATUS`) | Trạng thái tự động hoá được ghi tại thời điểm các test case tương ứng chưa tồn tại, không được rà soát lại sau khi test mới được viết thêm. | Cập nhật cả 3 mục sang dạng `"DONE, <tên file test>.test.tsx (<id thực trong file>)"`, khớp với sheet Automation Summary. | Đã sửa |
| 5 | **Test `CSET-6` (candidate-settings.test.tsx) fail thật khi chạy `npx vitest run`**: "Unable to find role=switch". Test mock `getCandidateSubscription` trả về Premium nhưng không đăng nhập giả lập, nên `CandidateSubscriptionProvider` (gate theo `userId` từ `useUser()`) không bao giờ gọi API subscription, `isPremium` luôn `false`, component render giao diện Free (khoá) thay vì Premium. 6 test còn lại trong cùng file vô tình không phụ thuộc state Premium nên vẫn pass, khiến lỗi bị lọt qua khi rà soát tài liệu. | `tests/unit/candidate-settings.test.tsx` | Test không seed `localStorage.interviewai_auth` và không mock `getCurrentUser`, nên `useUser()` không bao giờ resolve ra user có `id`, khiến effect fetch subscription trong `CandidateSubscriptionProvider` bị early-return. | Thêm `localStorage.setItem("interviewai_auth", "true")` + mock `getCurrentUser` trả về user có `id` trong `beforeEach`. Chạy lại toàn bộ suite xác nhận 535/535 pass. | Đã sửa |

### Bug sản phẩm thật, phát hiện qua việc đọc test file để viết tài liệu

| # | Bug | Vị trí | Nguyên nhân | Cách sửa | Trạng thái |
|---|---|---|---|---|---|
| 6 | **Admin Settings: nút "Save Permissions" không hoạt động.** Bấm vào không có phản hồi gì, không lưu, không báo lỗi. | `src/features/admin/components/settings/*` (tab Permissions) | Backend chưa có API lưu permissions, nhưng nút vẫn render ở trạng thái bấm được bình thường, click xong im lặng không làm gì (silent no-op). | Disable nút, thêm `title="Coming soon"` thay vì để active nhưng vô dụng. Test: `admin-platform-settings.test.tsx` (APS-5). | Đã sửa |
| 7 | **Admin Settings: nút "Save Notifications" không hoạt động**, tương tự bug #6, ở tab Notifications. | `src/features/admin/components/settings/*` (tab Notifications) | Backend chưa có API lưu notification preferences, nút vẫn active nhưng click không làm gì. | Disable nút, thêm `title="Coming soon"`. Test: `admin-platform-settings.test.tsx` (APS-7). | Đã sửa |
| 8 | **Admin Settings: nút "Reset Platform Data" ở danger zone không hoạt động**, đáng lo hơn 2 bug trên vì đây là hành động phá huỷ dữ liệu, nút active mà không làm gì dễ khiến admin tưởng đã reset thành công. | `src/features/admin/components/settings/*` (General, danger zone) | Backend chưa có API reset, nút vẫn active. | Disable nút, thêm `title="Coming soon"` thay vì để một hành động phá huỷ dữ liệu ở trạng thái mập mờ. Test: `admin-platform-settings.test.tsx` (APS-8). | Đã sửa |
| 9 | **Admin Content page: nút Delete từng dòng session không hoạt động.** | `src/features/admin/components/content/content-table.tsx` | Chưa có endpoint xoá content session, nút Trash2 vẫn active. | Disable nút, thêm `title="Coming soon"`. Test: `admin-content-table.test.tsx` (ACT-2). | Đã sửa |
| 10 | **Candidate Billing: nút "Manage Subscription" (Premium) không hoạt động.** | `src/features/candidate/components/billing/candidate-billing-page.tsx` | Chưa có trang/luồng quản lý subscription chi tiết phía backend, nút vẫn active. | Disable nút, thêm `title="Coming soon"`. Test: `candidate-billing.test.tsx` (CBILL-7). | Đã sửa |
| 11 | **Candidate Billing: nút "Update Billing Info" và "Change Payment Method" (Free) không hoạt động.** | `src/features/candidate/components/billing/candidate-billing-page.tsx` | Chưa có cổng cập nhật thông tin thanh toán, cả 2 nút vẫn active. | Disable cả 2 nút, thêm `title="Coming soon"`. Test: `candidate-billing.test.tsx` (CBILL-8). | Đã sửa |
| 12 | **Candidate Billing: nút Download hoá đơn là link chết khi hoá đơn không có `receiptUrl`.** | `src/features/candidate/components/billing/candidate-billing-page.tsx` | Component render một `<a>`/nút Download bất kể `receiptUrl` có tồn tại hay không, bấm vào không tải được gì. | Khi có `receiptUrl`: render `<a>` thật với `href` + `download`. Khi không có: render nút disabled + `title="Coming soon"` thay vì link chết. Test: `candidate-billing.test.tsx` (CBILL-5, CBILL-6). | Đã sửa |
| 13 | **HR Billing: cùng lỗi link chết như #12, ở màn hình billing của HR.** | `src/features/settings/components/hr-billing-subscription.tsx` | Giống #12 nhưng ở component HR riêng biệt. | Cùng cách sửa: `<a>` thật khi có `receiptUrl`, nút disabled + tooltip khi không có. Test: `hr-billing.test.tsx` (BILL-4b, BILL-4c). | Đã sửa |
| 14 | **HR Settings, tab Preferences: nút "Save Changes" không hoạt động.** | `src/features/settings/components/preferences-section.tsx` | Backend chưa có API lưu preferences, nút vẫn active. | Disable nút, thêm `title="Coming soon"`. Test: `hr-settings-preferences-notifications.test.tsx` (HRPREF-1). | Đã sửa |
| 15 | **HR Settings, tab Notifications: nút "Save Changes" không hoạt động.** | `src/features/settings/components/notifications-section.tsx` | Backend chưa có API lưu notification settings, nút vẫn active. | Disable nút, thêm `title="Coming soon"`. Test: `hr-settings-preferences-notifications.test.tsx` (HRNOTIF-1). | Đã sửa |
| 16 | **P0: Rò rỉ cache gói cước Premium giữa các user khác nhau trên cùng một trình duyệt** (lỗ hổng bảo mật/riêng tư trên máy dùng chung, ví dụ máy kiosk). Nếu user A (Premium) đăng xuất và user B đăng nhập trên cùng máy, B có thể tạm thời thấy giao diện Premium của A, thậm chí kích hoạt sai dialog "Premium revoked" cho người chưa từng có Premium. | `src/features/candidate/context/candidate-subscription-context.tsx` | Cache gói cước trong `localStorage` không gắn với user id nào cả, chỉ lưu 1 giá trị `PREMIUM`/`FREE` dùng chung cho mọi user trên máy, nên user sau load cache của user trước trong khoảng thời gian chờ API xác nhận lại. | Thêm key `hiregena-candidate-plan-user` lưu kèm user id; chỉ tin cache khi đúng user id hiện tại (`readCachedPlan`), sai thì mặc định FREE cho tới khi API xác nhận. Test hồi quy: `candidate-subscription-context.test.tsx` (CSUB-1 đến CSUB-4). | Đã sửa |
| 17 | **P0: Kết nối SignalR (`subscription-payment-hub`) throw lỗi đồng bộ làm crash toàn bộ cây component khi backend không hỗ trợ hub** (ví dụ môi trường candidate, hub chỉ dành cho HR). | `src/features/subscription/services/subscription-payment-hub.ts` | `createSubscriptionPaymentHubConnection()` throw exception ngay khi không dựng được kết nối (URL không hợp lệ/không cấu hình), trong khi hàm gọi nó không có try/catch bảo vệ nên lỗi văng lên tận React, sập cả trang. | Đổi hàm để trả về `null` thay vì throw khi dựng kết nối thất bại; nơi gọi kiểm tra `null` và fallback sang polling 30 giây. Test hồi quy: `subscription-realtime.test.tsx` (SUBRT-1, SUBRT-2). | Đã sửa |
| 18 | **P1: `UpgradeModal` âm thầm bỏ qua việc làm mới dữ liệu subscription sau khi thanh toán thành công nếu gặp lỗi mạng tạm thời.** User đã thanh toán xong nhưng giao diện vẫn hiển thị như chưa nâng cấp Premium. | `src/features/candidate/components/billing/upgrade-modal.tsx` | `finishPaid()` chỉ gọi API lấy subscription/usage/history mới đúng 1 lần sau khi xác nhận thanh toán; nếu lần gọi đó lỗi mạng, không có cơ chế thử lại, coi như mất luôn bản cập nhật. | Thêm retry tối đa 3 lần cho `finishPaid()`; nếu cả 3 lần đều lỗi thì bỏ qua trong im lặng (vì toast xác nhận thanh toán đã hiện rồi) thay vì crash. Test hồi quy: `candidate-upgrade-modal.test.tsx` (UPM-1, UPM-2). | Đã sửa |
| 19 | **HR Billing: hạ gói xuống Free ngay lập tức khi bấm "Downgrade to Free", thay vì giữ Premium tới hết kỳ đã thanh toán.** HR đã trả tiền cho cả tháng nhưng mất quyền lợi Premium ngay khi bấm huỷ, dù chưa hết hạn. | Backend (API cancel subscription), FE tại `src/features/settings/components/hr-billing-subscription.tsx` | Trước đây API cancel trả về `planCode=FREE` ngay lập tức khi resolve, FE áp thẳng giá trị đó vào state nên mất Premium tức thì. | Backend sửa để giữ `planCode=PREMIUM` (chỉ đổi `status` sang trạng thái "sẽ huỷ") cho tới khi hết kỳ thanh toán; FE không cần đổi gì vì `cancelPremium()` đã áp đúng theo `planCode` mà response trả về. Test hồi quy khoá lại hợp đồng này: `hr-billing.test.tsx` (BILL-7). | Đã sửa |

---

## Tổng kết số liệu

- **19 bug** phát hiện và đã sửa trong quá trình làm tài liệu test case (không liên quan sequence diagram): **5 bug** trong chính công cụ/tài liệu (#1-5), **14 bug** trong sản phẩm thật (#6-19), phát hiện qua việc đọc từng file `tests/unit/*.test.ts(x)` để viết tài liệu.
- **1 bug nghiêm trọng** (#1) là lỗi toàn vẹn dữ liệu trên chính file deliverable sống (`SU26SE102-GSU26SE52_QA_TestCases.xlsx`). Nếu không phát hiện qua audit hậu-kiểm (đếm sheet, kiểm tra trùng tên) thì sẽ để lại file bị nhân đôi dữ liệu mà không có dấu hiệu lỗi rõ ràng khi mở file.
- **1 bug thật trong bộ test tự động** (#5), phát hiện bằng cách thực sự chạy `npx vitest run` toàn bộ suite thay vì chỉ tin vào tài liệu đã ghi "Passed"; đã sửa và xác nhận lại 535/535 test pass.
- **3 bug mức P0/P1** (#16, #17, #18) là các lỗi nghiêm trọng thật của sản phẩm (rò rỉ cache Premium giữa user trên máy dùng chung, crash toàn trang khi SignalR không kết nối được, mất cập nhật trạng thái sau thanh toán) đã được sửa và có test hồi quy khoá lại, không phải chỉ sửa tạm.
- **10 bug dead-button** (#6-15) là các nút bấm được nhưng không làm gì (do backend chưa có API), nay đã disable kèm tooltip "Coming soon" thay vì để người dùng bấm vào khoảng không im lặng.
- Sau khi sửa các bug trên, đã tiếp tục bổ sung tài liệu cho toàn bộ các file test unit chưa được ghi vào `SU26SE102-GSU26SE52_QA_TestCases.xlsx` (208 scenario trên 26 file test mới, tổng cộng test case trong file lên **208 scenario / 212 sheet**), xác minh 0 sheet trùng tên và 0 ID trùng sau mỗi đợt.
