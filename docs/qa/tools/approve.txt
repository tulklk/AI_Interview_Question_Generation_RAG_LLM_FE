The "Approve Plan" button looked disabled when required fields were missing, yet stayed clickable and did nothing: no longer applicable - the Studio action bar was redesigned and the Approve Plan action is enabled whenever it is shown. Clicking it first saves any pending plan settings; if that save fails, an error toast is shown and the approval stops. The only silent exit left is when the project settings have not been loaded yet.
