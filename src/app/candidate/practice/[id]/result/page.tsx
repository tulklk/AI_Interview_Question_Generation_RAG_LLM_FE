import { FeedbackResultClient } from "@/features/candidate/components/feedback/feedback-result-client";

// Dynamic route renders on demand at runtime. `id` here is the practice session id.
export default function FeedbackResultPage() {
  return <FeedbackResultClient />;
}
