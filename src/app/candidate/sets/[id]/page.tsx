import { SetDetailClient } from "@/features/candidate/components/sets/set-detail-client";

// Dynamic route renders on demand at runtime.
export default function SetDetailPage() {
  return <SetDetailClient />;
}
